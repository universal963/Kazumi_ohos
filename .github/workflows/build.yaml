name: build

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
    paths-ignore:
      - 'static/**'
      - '**.md'
      - '.gitignore'
      - '.github/ISSUE_TEMPLATE/**'
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'warning'
        type: choice
        options:
          - info
          - warning
          - debug

jobs:
  flutter_build_ohos:
    name: Release for ohos
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y curl xz-utils zip libglu1-mesa
        shell: bash
      - name: Setup JDK17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Setup HarmonyOS toolchain
        run: |
          mkdir -p ohos-sdk
          cd ohos-sdk
          curl -L https://github.com/ErBWs/ohos-sdk/releases/download/5.0.13.200/ohos-sdk-linux-amd64.tar.gz.aa
          curl -L https://github.com/ErBWs/ohos-sdk/releases/download/5.0.13.200/ohos-sdk-linux-amd64.tar.gz.ab
          curl -L https://github.com/ErBWs/ohos-sdk/releases/download/5.0.13.200/ohos-sdk-linux-amd64.tar.gz.sha256
          
          cat ohos-sdk-linux-amd64.tar.gz.aa ohos-sdk-linux-amd64.tar.gz.ab > ohos-sdk-linux-amd64.tar.gz
          sha256sum -c ohos-sdk-linux-amd64.tar.gz.sha256
          tar -xzf ohos-sdk-linux-amd64.tar.gz
          
          echo "$GITHUB_WORKSPACE/ohos-sdk/ohpm/bin" >> $GITHUB_PATH
          echo "$GITHUB_WORKSPACE/ohos-sdk/hvigor/bin" >> $GITHUB_PATH
          echo "$GITHUB_WORKSPACE/ohos-sdk/tool/node/bin" >> $GITHUB_PATH
          echo "$GITHUB_WORKSPACE/ohos-sdk/bin" >> $GITHUB_PATH
          echo "OHOS_NDK_HOME=$GITHUB_WORKSPACE/ohos-sdk/sdk/default/openharmony" >> $GITHUB_ENV
        shell: bash
      - name: Setup flutter-ohos
        run: |
          git clone https://gitcode.com/openharmony-tpc/flutter_flutter.git $GITHUB_WORKSPACE/flutter
          cd flutter
          git switch oh-3.27.4-dev
          git pull origin
          echo "$GITHUB_WORKSPACE/flutter/bin" >> $GITHUB_PATH
        shell: bash
      - name: Get Flutter dependencies
        run: flutter pub get
        shell: bash
      - name: Print Flutter version
        run: flutter doctor -v
        shell: bash
      - name: Build Flutter for ohos
        run: flutter build hap --release
        shell: bash
      - name: Package ohos build output
        run: cp ./ohos/entry/build/default/outputs/default/entry-default-unsigned.hap Kazumi_ohos_canary.hap
        shell: bash
      - name: Upload ohos outputs
        uses: actions/upload-artifact@v4
        with:
          name: ohos_outputs
          path: Kazumi_ohos_*.hap
